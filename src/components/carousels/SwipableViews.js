

/**
 *
 * SwipableViews
 *
 *
 * [{controller, method},{controller, method},{controller, method},{controller, method}]
 *
 * [ViewConstructor, ViewConstructor, ViewConstructor, ViewConstructor]
 *
 *
 * controller/method would be cooler
 * - but would have to be hooked into routing
 * - have to have route listeners - ie if the user navigates directly to this page
 * - subviews ?
 * /Home/Index/1
 * /Home/Index/2
 * /Home/Index/3
 * would probably have to overwrite the router in some way.
 *
 *
 *
 *
 *
 */


class SwipableViews extends View {
    // starts the app on
}