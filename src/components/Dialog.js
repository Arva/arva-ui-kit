/**
 * Created by lundfall on 07/07/16.
 */
/*
import Surface              from 'famous/core/Surface.js';

import {layout, options}    from 'arva-js/layout/decorators.js';
import {View}               from 'arva-js/core/View.js';

@layout.margins([30])
export class Dialog extends View {
    @layout.fullscreen
    background = new Surface({properties: {backgroundColor: 'white'}});

    @layout.dock('top', ~50)
    title = new Surface({content: this.options.title});

    @layout.dock('top', ~50, 8)
    body = new Surface({content: this.options.body});


    constructor(options){
        super(options);
        this.layout.on('layoutstart', ({size}) => {
            let marginSize = Math.max((size[0] - 320)/2, 32);
            this.decorations.viewMargins = [marginSize, marginSize, marginSize, marginSize];
        });
    }

}*/
